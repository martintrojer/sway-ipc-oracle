# i3 adapter

The i3 adapter is i3's own `complete-run.pl`, unchanged, using Xvfb and Xephyr.
The runner checks that both its source and binary are from the vendored suite's
pinned commit before invoking it.
